void main()
{
  dynamic a=100,b=20;
 print ("a = b -> ${a = b}"); //a=b,  a=20
 print ("a += b -> ${a += b}"); //a=a+b, a=20+20=40
 print ("a -= b -> ${a -= b}");
 print ("a *= b -> ${a *= b}");
 print ("a /= b -> ${a /= b}");
 print ("a %= b -> ${a %= b}");
 print ("a ~/= b -> ${a ~/= b}");
}